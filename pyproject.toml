[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "molmoact2-local"
version = "0.1.0"

[tool.setuptools]
packages = ["cameras", "gripper", "inference", "robot"]